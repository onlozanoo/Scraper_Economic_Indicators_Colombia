# conda env create --file environment.yml
name: scraper_indicadores_economicos
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.7
  - pip:
    - pyhere
    